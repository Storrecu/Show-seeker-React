import React from 'react';

const AppInfo = () => {
  return (
    <h2 className="info-text">
      <i className="fa-solid fa-quote-left"></i>Wondering if there's a platform
      to search, add favorite series, and easily recommend shows to friends?
      Show Seeker is the solution, preventing the trouble of forgetting show
      names when making recommendations
      <i className="fa-solid fa-quote-right"></i>
    </h2>
  );
};

export default AppInfo;
